## Getting started

# Install the (pre-compiled) multinma package and dependencies
install.packages("multinma", type = "binary", dependencies = TRUE)

# The package contains several vignettes with example analyses
browseVignettes("multinma")

